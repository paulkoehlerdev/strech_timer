import 'package:flutter/cupertino.dart';

class TimeslotElementWidget extends StatelessWidget {
  const TimeslotElementWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
